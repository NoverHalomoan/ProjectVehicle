spring:
  config:
    import: optional:env[.env]

  datasource:
    url: jdbc:postgresql://localhost:5438/microservicesDB
    username: jasperApp
    password: jasperApp
    driver-class-name: org.postgresql.Driver
  graphql:
    schema:
      locations:
        -none
        #- classpath:graphql/
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      repositories:
        enabled: true
    mongodb:
      uri: ${MONGO_DB:mongodb+srv://vernover:noverver@database-course-cluster.0ahanag.mongodb.net/KendaraanDB?retryWrites=true&w=majority&appName=database-course-Cluster}
      database: ${DB_MONGO:KendaraanDB}
  mail:
    host: ${MAIL_HOST_DATA:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:sellonepython@gmail.com}
    password: ${MAIL_PASSWORD:vgcstyozwxjmeylk}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  JWT_SECRET: ${JWT_SECRET:JOBPORTAL-RFsAfqfwq-+d8RYCPiC~&Q_E?^Z1dxq|{k.dsfadsfgeh78ffffMc& I-*TZb(0:8=m2m}SEOH6v)`R.XHQZ}
  JWT_ACTIVATION: ${JWT_ACTIVATION:ayLGrXGXho8qw1Wo7sWXgtrVqCZLBXSWre02uwxtRfId3o3womF9IJYuRzCYVyZa}
  JWT_UPDATE_PROFILE: ${JWT_UPDATE_PROFILE:J4R34rjwkKEgKcaZlsSHuA5SoHXv4woKoDDG1gykpT78tqyrT2xcQCPV4U1rdb5S}
  issuer: VehicleBucket

server:
  port: 8080
app:
  baseUrl: http://localhost:8080

logging:
  level:
    root: info
    org.springframework: info
    org.springframework.beans.factory.support: DEBUG
